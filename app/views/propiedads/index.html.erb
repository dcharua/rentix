  <div class="wrapper wrapper-content  animated fadeInRight">
      <div class="row">
          <div class="col-sm-12">
              <div class="ibox">
                  <div class="ibox-content">
                    <div class="ibox-tools">
                        <a href="<%=new_propiedad_path%>" class="btn btn-primary btn-xs">Agregar propiedad</a>
                    </div>
                      <h2>Propiedades</h2>
                      <p>
                          Estos son todas tus propiedades, haz click en editar para modificar sus datos
                      </p>
                      <%= render 'lookup' %>

                      <%= render 'list' %>

                  </div>
              </div>
          </div>
        </div>
    </div>
