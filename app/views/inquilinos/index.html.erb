

  <div class="wrapper wrapper-content  animated fadeInRight">
      <div class="row">
          <div class="col-sm-12">
              <div class="ibox">
                  <div class="ibox-content">
                    <div class="ibox-tools">
                        <a href="<%=new_inquilino_path%>" class="btn btn-primary btn-xs">Agregar inquilino</a>
                    </div>
                      <h2>Inquilino</h2>
                      <p>
                          Estos son todos tus inquilino, haz click en editar para modificar sus datos
                      </p>
                      <%= render 'lookup' %>

                      <%= render 'list' %>

                  </div>
              </div>
          </div>
        </div>
    </div>
