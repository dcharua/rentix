  <div class="wrapper wrapper-content  animated fadeInRight">
      <div class="row">
          <div class="col-sm-12">
              <div class="ibox">
                  <div class="ibox-content">
                    <div class="ibox-tools">
                        <a href="<%=new_renta_path%>" class="btn btn-primary btn-xs">Agregar renta</a>
                    </div>
                      <h2>Rentas</h2>
                      <p>
                          Estos son todas tus rentas, haz click en editar para modificar sus datos
                      </p>
                      <%= render 'lookup' %>

                      <%= render 'list' %>
                  </div>
              </div>
          </div>
        </div>
    </div>
